import { NextPage } from 'next';
import { Layout } from '../../components/layout/layout';

const CustomersPage: NextPage = () => {
  return (
    <Layout>
      <div className="m-8 underline">Customers Page</div>
    </Layout>
  );
};

export default CustomersPage;
