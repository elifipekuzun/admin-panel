import { NextPage } from 'next';
import { Layout } from '../../components/layout/layout';

const ProductsPage: NextPage = () => {
  return (
    <Layout>
      <div className="m-8 underline">Products Page</div>
    </Layout>
  );
};

export default ProductsPage;
