import { NextPage } from 'next';
import { Layout } from '../../components/layout/layout';

const OrdersPage: NextPage = () => {
  return (
    <Layout>
      <div className="m-8 underline">Orders Page</div>
    </Layout>
  );
};

export default OrdersPage;
